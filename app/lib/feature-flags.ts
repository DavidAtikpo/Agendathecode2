/**
 * Mettre à `true` lorsque l’offre Pro est prête (Stripe + fonctionnalités).
 * Quand `false` : pas de bouton / bandeau pour souscrire, et `POST /api/billing/checkout` est refusé.
 */
export const PRO_SUBSCRIPTION_SALES_ENABLED = false;
